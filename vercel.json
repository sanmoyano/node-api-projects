{
  "version":2,
  "builds": [
      {
        "src": "./index.js",
        "use": "@vercel/node"
      }
    ],
  "routes":[
      {
        "src":"/api/projects",
        "dest":"/api/projects"
      },
      {
        "src":"/api/projects/:id",
        "dest":"/api/projects/:id"
      }
    ]
}